const indexPage = "index.html";
const writerPage = "writer.html";
const readerPage = "reader.html";

const pageTitles = [
  indexPage,
  writerPage,
  readerPage,
];

const updated = "Updated at:";
const stored = "Stored at:";

const timeTexts = [
    updated,
    stored,
];

const addButton = "add";
const removeButton = "remove";

const buttons = [
    addButton, 
    removeButton,
];

window.userText = {
    pageTitles,
    timeTexts,
    buttons,
};
